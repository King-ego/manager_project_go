name: CI / Build and Test / docker
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [ main, develop ]
jobs:
  docker:
    if: |
      github.ref == 'refs/heads/main' ||
      github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Copiar .env.example to .env
        run: cp .env.example .env

      - name: 🐳 Subir Docker Compose
        run: docker compose -f docker-compose.yml up -d --build

      - name: 🕒 Aguardar serviços subirem
        run: |
            echo "Aguardando Postgres e Redis..."
            sleep 20

      - name: 📋 Verificar containers ativos
        run: docker ps

      - name: Install PostgreSQL client
        run: sudo apt-get install -y postgresql-client

      - name: 🔍 Testar rota /health
        run: |
          echo "Aguardando inicialização dos serviços..."
          sleep 30  # Aguarde um tempo adicional antes de começar os testes

          echo "Testando rota /health..."
          for i in {1..10}; do  
            if curl -s http://localhost:8989/health | grep -q "healthy"; then
              echo "✅ Serviço respondeu saudável!"
              exit 0
            fi
            echo "Tentativa $i/10 - Aguardando serviço iniciar..."
            sleep 10  
          done
          echo "❌ Serviço não respondeu a tempo."

          echo "=== LOGS DO BANCO ==="
          docker compose logs manager_db
          echo "=== LOGS DA APLICAÇÃO ==="
          docker compose logs manager_project
          exit 1
